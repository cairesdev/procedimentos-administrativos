import { WorkspaceShell } from "@/shared/workspace/WorkspaceShell";

export default function AlmoxarifadoLayout({ children }: { children: React.ReactNode }) {
  return <WorkspaceShell workspaceId="almoxarifado">{children}</WorkspaceShell>;
}
